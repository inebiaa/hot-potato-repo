/**
 * Feed card width spine: masonry lanes, overlay close-up, and embed should match.
 * Twitter-style open show uses the same card at the same max width as the grid pin.
 */
export const EVENT_FEED_CARD_MAX_WIDTH_PX = 448;

/** Tailwind `max-w-md` (28rem) at default root font size. */
export const EVENT_FEED_CARD_MAX_WIDTH_CLASS = 'max-w-md';
